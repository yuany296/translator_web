
## 开发要求

1. **修改后要自动提交到本地 Git**

每次完成修改并验证通过后，需要执行：

```bash
git add .
git commit -m "说明本次修改内容"
```

只提交到本地，不要自动推送远程仓库。

2. **遇到问题时不要直接乱改**

如果出现 bug，必须按下面流程处理：

```text
先分析问题
  ↓
添加必要的 debug 日志或可视化工具
  ↓
自己运行并复现问题
  ↓
根据日志找到真正出错的位置
  ↓
再修改代码
  ↓
重新运行验证
  ↓
验证通过后本地 commit
```

3. **不要做的事**

* 不要没找到原因就改代码。
* 不要一次改很多无关内容。
* 不要没运行就说修好了。
* 不要把临时 debug 代码长期留在正式逻辑里，除非有 debug 开关。
* 不要自动 push 到远程。
