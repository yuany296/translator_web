export function installBackground31(runtime) {
  // Glossary REST API helpers (for server-side storage via SQLite)
  const GLOSSARY_API_TIMEOUT_MS = 10000;
  runtime.GLOSSARY_API_TIMEOUT_MS = GLOSSARY_API_TIMEOUT_MS;
}
