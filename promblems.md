1. ~~有的文字会识别两遍？这是为什么（改为fast √）~~
2. 网站上的文字会被截断，有什么方法可以改善。（还没测试）
   当前 KakaoPage 漫画由多张连续 `<img>` 组成，禁止逐张独立 OCR，因为文字可能跨越图片切片边界。请实现跨图 OCR：按照 DOM 顺序获取相邻漫画原图，统一到相同宽度，对每张图拼接上一张底部 300px、当前图、下一张顶部 300px 后执行 OCR。将检测框转换为漫画容器的全局坐标，并通过 IoU 和文本相似度去重。识别结果归属以文本框中心点所在图片为准，但覆盖层使用整个漫画容器上的统一绝对定位层，使跨图片边界的文字框也能完整覆盖。不得通过调低阈值或扩大单图检测框伪解决切断问题。
3. 背后虽然是白色的，但是本身没有对话框。但是还是是白色，应该怎么办？还有框框有时候会超出白色对话框。我觉得背景是纯色的可以直接把框框背景设置为那样，但是背景太复杂了，就直接黑字加边缘了。
4. 我觉得翻译太慢了，能不能自动直接翻译下去。把这个设置为一个可自选的模式什么的。（这个好像也没有呢）
5. 覆盖层是和背景颜色一样是黑色的了，但是字体是深蓝色，结果就是看不见翻译。字体的颜色也要和原文一致，但是有些有很多颜色，所以这个只要背景和字体颜色拉开就行了。
   比如说下面这张图。
6. 旁白和对话框分得不是很清。
7. 合并得不是很好。

# 6.20日

1. 出现了这样的截图。![1781936334360](image/promblems/1781936334360.png)

![1781936282048](image/promblems/1781936282048.png)
