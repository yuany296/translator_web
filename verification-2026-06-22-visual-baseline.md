# 视觉基线与跨图回退验证记录

日期：2026-06-22
执行者：Codex

## 本次变更

- KakaoPage 相邻图片只有在不同源、宽度接近、水平对齐且接缝不超过 32px 时才参与跨图 OCR。
- 上下文窗口由固定 300px 改为按图片显示比例动态计算，并限制在 96–300px。
- stitch 元数据记录捕获时的页面全局坐标，去重不再依赖滚动后的临时 DOM 坐标。
- 拼接请求异常、失败、owner 无文本或坐标明显越界时自动回退当前单图。
- 新增三张浏览器真实 OCR 输入及四阶段视觉回归指标。

## 自动验证

- Node 语法与根目录运行时：34 passed。
- Python OCR、背景分析与真实截图回归：30 passed。
- 根目录扩展构建：通过，`dist/` 由根目录实现生成。
- `git diff --check`：通过。

## 固定截图结果

| 用例 | OCR items | 区域 | 重复率 | 越界框 | 最大区域面积比 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 白色气泡 | 3 | 1 | 0 | 0 | 16.3% |
| 空白边界 | 0 | 0 | 0 | 0 | 0 |
| 复杂背景标题 | 5 | 0 | 0 | 0 | 0 |

## 已知说明

- PaddleOCR 环境输出 6 条依赖/弃用警告，但测试全部通过。
- 复杂背景标题的服务原始结果仍包含两个独立的单字符 `D`；前端现有无意义单字母过滤会在渲染前丢弃它们。
